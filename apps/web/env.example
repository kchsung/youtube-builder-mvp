## Vite (client) 환경변수 예시
## 실제 값은 Supabase 프로젝트 설정에서 가져와서 채워주세요.
##
## 사용 방법:
## - 이 파일을 `apps/web/.env.local`로 복사한 뒤 값을 채우세요.
## - (주의) 이 리포지토리는 dotfile 추가가 제한될 수 있어 예시 파일은 dot 없이 제공합니다.

# Supabase Project URL (예: https://xxxx.supabase.co)
VITE_SUPABASE_URL=

# Supabase anon public key (JWT)
VITE_SUPABASE_ANON_KEY=

# 선택: Edge Functions 베이스 URL을 직접 지정하고 싶을 때만 사용
# 기본값은 `${VITE_SUPABASE_URL}/functions/v1`
VITE_SUPABASE_FUNCTIONS_BASE=



