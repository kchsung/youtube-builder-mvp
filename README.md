# youtube-generator

YouTube 영상 생성(TrendStory) MVP 프로젝트입니다.

## 구조
- `apps/web`: Vite + React 프론트엔드
- `supabase/functions`: Supabase Edge Functions (백엔드 워크플로우)
- `supabase/migrations`: DB 마이그레이션

## 주의
- `apps/web/env.local` 및 `.env*` 파일은 **커밋하지 않습니다** (비밀키/토큰 포함 가능)



