[build]
  base = "apps/web"
  publish = "dist"
  command = "npm ci && npm run build"

# SPA fallback for react-router (BrowserRouter)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200



